base_url: https://api.openai.com/v1
api_key: YOUR_API_KEY_HERE
text_generation:
  model_id: gpt-5.4
  prompt: "You are a brief, but helpful bot called {{ baibot_name }} powered by the {{ baibot_model_id }} model. The date/time of this conversation's start is: {{ baibot_conversation_start_time_utc }}."
  temperature: 1.0
  # Reasoning models need to use `max_completion_tokens` instead of `max_response_tokens`.
  # If you're dealing with a non-reasoning model, specify `max_response_tokens` and unset `max_completion_tokens`.
  max_response_tokens: null
  max_completion_tokens: 128000
  max_context_tokens: 400000
  # Built-in tools
  tools:
    web_search: false
    code_interpreter: false
speech_to_text:
  model_id: whisper-1
text_to_speech:
  model_id: tts-1-hd
  voice: onyx
  speed: 1.0
  response_format: opus
image_generation:
  # Sunburst supports image generation and precise editing.
  # Use gpt-image-2.5-flare for faster generation.
  model_id: gpt-image-2.5-sunburst
  # GPT image models do not use DALL-E's style parameter; describe the style in the prompt.
  style: null
  # null lets the model choose; examples: 1024x1024, 1536x1024, 1024x1536, 2048x2048.
  size: null
  # null uses the model default (auto). Options: low, medium, high, xhigh, max, auto.
  quality: null
